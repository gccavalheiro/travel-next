import styled from 'styled-components'
import theme from 'styles/theme/theme'

export const Container = styled.div`
  max-width: 968px;
  margin-left: ${theme.spacings.mb1};
  margin-right: ${theme.spacings.mb1};
`
